spring.application.name=furtureforwarddemo

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update

springdoc.api-docs.title=FutureForward Banking API
springdoc.api-docs.version=OPENAPI_3_0
springdoc.api-docs.description=Banking system with Customer, Account, and Transaction management


# TODO: Add in the MySQL configuration once ready as well as the Hibernate JPA config
# MySQL DB config
# spring.datasource.url=jdbc:mysql://localhost:3306/your_database_name
# spring.datasource.username=your_username
# spring.datasource.password=your_password
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate JPA config 
# spring.jpa.hibernate.ddl-auto=update
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
